import React from "react";
import styles from "./Nav.module.css";

export const Nav = () => {
  return (
    <nav className={styles.nav}>
      <h1 className={styles.title}>Track yo' runs</h1>
    </nav>
  );
};
